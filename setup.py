from setuptools import setup, find_packages

setup(
    name="ai_agent_meta_cognitive",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # dependencies will be handled by requirements.txt
    ],
    package_dir={'': '.'},
)
