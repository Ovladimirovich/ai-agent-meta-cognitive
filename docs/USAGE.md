# 🚀 Руководство по использованию мета-когнитивного AI агента

## 📋 Обзор возможностей

Мета-когнитивный AI агент предоставляет широкий спектр возможностей для работы с искусственным интеллектом, включая самосознание, самообучение и мета-познание. Ниже приведены основные способы использования системы.

## 🛠️ Базовое использование

### 1. Инициализация агента

#### Через API
```python
import httpx

async def use_agent():
    async with httpx.AsyncClient() as client:
        # Отправка запроса к агенту
        response = await client.post(
            "http://localhost:8000/agent/process",
            json={
                "query": "Проанализируй эту задачу и предложи решение",
                "context": {
                    "domain": "анализ данных",
                    "complexity": "средняя",
                    "deadline": "2024-12-31"
                }
            },
            headers={"Content-Type": "application/json"}
        )
        
        result = response.json()
        print(f"Результат: {result['response']}")
        print(f"Уверенность: {result['confidence']}")
        print(f"Обоснование: {result['reasoning']}")
```

#### Через Python SDK
```python
from ai_agent_meta_cognitive.agent.core import AgentCore
from ai_agent_meta_cognitive.config import AgentConfig

# Создание конфигурации
config = AgentConfig(
    confidence_threshold=0.7,
    max_execution_time=30.0,
    enable_reasoning_trace=True
)

# Инициализация агента
agent = AgentCore(config)

# Обработка запроса
response = await agent.process_request({
    "query": "Как оптимизировать производительность системы?",
    "context": {"domain": "системное администрирование"},
    "preferences": {"response_format": "structured"}
})

print(f"Результат: {response.result}")
print(f"Уверенность: {response.confidence}")
print(f"Шаги рассуждения: {len(response.reasoning_trace)}")
```

## 🔍 Продвинутое использование

### 1. Работа с инструментами

#### Использование RAG
```python
from ai_agent_meta_cognitive.agent.tools import RAGTool

rag_tool = RAGTool()
result = await rag_tool.execute({
    "query": "Какие существуют методы оптимизации памяти?",
    "context_size": 5,
    "similarity_threshold": 0.7
})
```

#### Использование гибридных моделей
```python
from ai_agent_meta_cognitive.agent.tools import HybridModelsTool

hybrid_tool = HybridModelsTool()
result = await hybrid_tool.execute({
    "query": "Создай креативный маркетинговый план",
    "model_preference": "creative",  # creative, analytical, balanced
    "temperature": 0.8
})
```

#### Использование кэширования
```python
from ai_agent_meta_cognitive.agent.tools import CacheTool

cache_tool = CacheTool()
# Кэширование результата
await cache_tool.set("query_hash", result, ttl=3600)

# Получение из кэша
cached_result = await cache_tool.get("query_hash")
```

### 2. Самоанализ и мета-познание

#### Получение статуса агента
```python
status = await agent.get_status()
print(f"Состояние: {status.state}")
print(f"Уровень уверенности: {status.confidence}")
print(f"Активные инструменты: {status.active_tools}")
```

#### Запуск процесса самоанализа
```python
reflection_result = await agent.reflect_on_interaction({
    "query": "Как можно улучшить ответ на этот запрос?",
    "previous_response": previous_result,
    "user_feedback": "ответ был полезен, но не хватило примеров"
})
```

### 3. Обучение и адаптация

#### Обучение на примере
```python
experience = {
    "input": "Пользователь спросил о Python",
    "output": "Предоставлен подробный ответ о Python",
    "feedback": "positive",
    "context": {"domain": "программирование", "difficulty": "beginner"}
}

await agent.learn_from_experience(experience)
```

## 🌐 API Эндпоинты

### Основные эндпоинты

#### Обработка запроса
```
POST /agent/process
Content-Type: application/json

{
  "query": "Текст запроса пользователя",
  "context": {
    "domain": "область знаний",
    "complexity": "сложность",
    "deadline": "срок выполнения"
  },
  "preferences": {
    "response_format": "simple|detailed|structured",
    "creativity_level": 0.1-0.9,
    "factuality_requirement": 0.1-0.9
  }
}
```

#### Получение статуса агента
```
GET /agent/status
```

#### Проверка здоровья
```
GET /agent/health
```

#### Получение мета-информации
```
GET /agent/insights
```

### Примеры запросов

#### Простой запрос
```bash
curl -X POST "http://localhost:8000/agent/process" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Как работает мета-когнитивный агент?",
    "context": {"domain": "искусственный интеллект"}
  }'
```

#### Запрос с контекстом
```bash
curl -X POST "http://localhost:8000/agent/process" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Оптимизируй производительность системы",
    "context": {
      "domain": "системное администрирование",
      "complexity": "высокая",
      "constraints": ["ограничения по ресурсам", "безопасность"]
    },
    "preferences": {
      "response_format": "structured",
      "creativity_level": 0.5
    }
  }'
```

## 🧠 Мета-когнитивные возможности

### 1. Самоосознание
Агент может оценить свои текущие способности и ограничения:

```python
self_assessment = await agent.self_assess({
    "task": "анализ временных рядов",
    "required_skills": ["статистика", "прогнозирование"],
    "available_tools": ["аналитика", "веб-поиск"]
})
```

### 2. Оценка уверенности
Агент возвращает уровень уверенности в своих ответах:

```python
response = await agent.process_request({"query": "Сложный вопрос"})
print(f"Уверенность: {response.confidence}")
if response.confidence < 0.7:
    print("Ответ может быть неточным, требуется дополнительная проверка")
```

### 3. Обоснование решений
Агент предоставляет пошаговое обоснование своих решений:

```python
response = await agent.process_request({
    "query": "Какой подход выбрать для решения задачи?"
})
for step in response.reasoning_trace:
    print(f"Шаг {step.step_number}: {step.description}")
    print(f"  Уверенность: {step.confidence}")
    print(f"  Использованные инструменты: {step.tools_used}")
```

## 📊 Мониторинг и аналитика

### Получение метрик производительности
```bash
curl -X GET "http://localhost:8000/agent/metrics"
```

### Получение отчета о здоровье
```bash
curl -X GET "http://localhost:8000/agent/health-report"
```

### Логирование взаимодействий
Агент автоматически логирует все взаимодействия с метаданными:

- Время выполнения
- Использованные инструменты
- Уровень уверенности
- Обоснование решений
- Обратная связь пользователя

## 🔧 Настройка и конфигурация

### Уровни уверенности
```python
# Высокая уверенность (0.8-1.0)
# Агент предоставляет прямые ответы без дополнительных проверок

# Средняя уверенность (0.5-0.79)
# Агент указывает на неопределенность и может использовать дополнительные инструменты

# Низкая уверенность (0.0-0.49)
# Агент рекомендует человеческое вмешательство или дополнительную проверку
```

### Сложность задач
```python
# Простые задачи
# Быстрые ответы с минимальным использованием инструментов

# Средние задачи
# Использование 1-2 инструментов, базовый анализ

# Сложные задачи
# Многошаговый анализ, использование нескольких инструментов, глубокое рассуждение
```

## 🤝 Интеграция с внешними системами

### Webhook интеграция
```python
# Настройка webhook для получения уведомлений
await agent.setup_webhook({
    "url": "https://your-app.com/webhooks/agent-updates",
    "events": ["task_completed", "learning_occurred", "health_issues_detected"]
})
```

### База данных интеграция
```python
from ai_agent_meta_cognitive.database import PostgreSQLManager

db_manager = PostgreSQLManager("postgresql://user:pass@localhost/db")
await agent.connect_to_database(db_manager)
```

## 🛡️ Безопасность и приватность

### Обработка чувствительных данных
Агент автоматически обнаруживает и защищает чувствительные данные:

- Персональная информация
- Ключи API
- Конфиденциальные документы
- Финансовая информация

### Проверка авторизации
```python
# Каждый запрос может требовать авторизации
response = await agent.process_request({
    "query": "Конфиденциальная информация",
    "auth_token": "valid_token",
    "permissions": ["read:confidential"]
})
```

## 📈 Примеры практических применений

### 1. Анализ данных
```python
response = await agent.process_request({
    "query": "Проанализируй эти данные и найди паттерны",
    "context": {"domain": "анализ данных"},
    "attachments": [
        {"type": "csv", "data": "base64_encoded_csv_data"},
        {"type": "json", "data": "base64_encoded_json_data"}
    ]
})
```

### 2. Генерация контента
```python
response = await agent.process_request({
    "query": "Создай статью на тему 'Искусственный интеллект в 2025 году'",
    "context": {"domain": "маркетинг", "target_audience": "эксперты"},
    "preferences": {
        "creativity_level": 0.8,
        "tone": "профессиональный",
        "length": "длинная статья"
    }
})
```

### 3. Решение проблем
```python
response = await agent.process_request({
    "query": "У нас возникла проблема с производительностью системы",
    "context": {
        "domain": "системное администрирование",
        "problem_description": "медленная работа базы данных",
        "system_specs": {"ram": "16GB", "cpu": "8 cores", "storage": "SSD"}
    }
})
```

## 🚀 Производительность и оптимизация

### Кэширование
Агент автоматически кэширует частые запросы и результаты:

- Запросы с аналогичным смыслом
- Результаты аналитических инструментов
- Часто используемые паттерны

### Параллельная обработка
Для сложных задач агент может использовать параллельную обработку:

```python
response = await agent.process_request({
    "query": "Выполни несколько независимых анализов",
    "parallel_tasks": [
        {"type": "sentiment_analysis", "data": "..."},
        {"type": "trend_analysis", "data": "..."},
        {"type": "pattern_recognition", "data": "..."}
    ]
})
```

## 📞 Поддержка и сообщество

Для получения помощи и участия в сообществе:

- [GitHub Issues](https://github.com/your-repo/issues) - для сообщений об ошибках
- [Discussions](https://github.com/your-repo/discussions) - для обсуждений
- [Documentation](https://your-docs.com) - полная документация
- [Examples](https://github.com/your-repo/examples) - примеры использования

---

*Это руководство охватывает основные аспекты использования мета-когнитивного AI агента. Для более глубокого понимания отдельных компонентов обратитесь к соответствующим разделам документации.*