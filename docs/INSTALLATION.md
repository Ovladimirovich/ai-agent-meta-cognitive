# ⚙️ Установка и настройка мета-когнитивного AI агента

## 📋 Системные требования

### Минимальные требования
- **Операционная система**: Windows 10+, macOS 10.15+, Linux (Ubuntu 20.04+)
- **Процессор**: 4 ядра, 2.4 ГГц
- **Оперативная память**: 8 ГБ
- **Свободное место**: 10 ГБ
- **Python**: 3.9+

### Рекомендуемые требования
- **Операционная система**: Linux (Ubuntu 22.04+) или Windows 11
- **Процессор**: 8 ядер, 3.0 ГГц
- **Оперативная память**: 16 ГБ+
- **GPU**: NVIDIA RTX 3080+ (для локальных моделей)
- **Свободное место**: 50 ГБ+
- **Python**: 3.10+

## 🔧 Установка зависимостей

### Виртуальное окружение (рекомендуется)
```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# Windows
venv\Scripts\activate
# Linux/macOS
source venv/bin/activate

# Обновление pip
pip install --upgrade pip
```

### Установка основных зависимостей
```bash
# Установка основных зависимостей
pip install -r requirements.txt

# Установка зависимостей для разработки (опционально)
pip install -r requirements-dev.txt

# Установка дополнительных зависимостей (опционально)
pip install -r requirements-optional.txt
```

## 🚀 Быстрая установка

### С использованием Make (Linux/macOS)
```bash
# Полная установка
make setup

# Запуск в режиме разработки
make run-dev

# Запуск тестов
make test-all
```

### С использованием скрипта (Windows)
```bash
# Установка и запуск
.\activate_and_install.bat
```

## 🐳 Установка с использованием Docker

### Сборка образа
```bash
# Сборка основного образа
docker build -t ai-agent-meta-cognitive .

# Сборка с аргументами
docker build -t ai-agent-meta-cognitive \
  --build-arg PYTHON_VERSION=3.10 \
  --build-arg REQUIREMENTS_FILE=requirements.txt .
```

### Запуск контейнера
```bash
# Запуск с монтированием данных
docker run -d \
  --name ai-agent \
  -p 8000:8000 \
  -v ./data:/app/data \
  -v ./models:/app/models \
  -e OPENAI_API_KEY=your_key_here \
  -e GOOGLE_API_KEY=your_key_here \
  ai-agent-meta-cognitive
```

### Docker Compose
```bash
# Запуск всей инфраструктуры
docker-compose up -d

# Запуск только агента
docker-compose up agent -d
```

## 🔑 Настройка конфигурации

### Создание файла конфигурации
```bash
# Копирование примера конфигурации
cp .env.example .env
```

### Основные параметры конфигурации
```bash
# API ключи
OPENAI_API_KEY=your_openai_key_here
GOOGLE_API_KEY=your_google_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here

# Настройки базы данных
DATABASE_URL=postgresql://user:password@localhost:5432/agent_db
REDIS_URL=redis://localhost:6379

# Настройки агента
AGENT_CONFIDENCE_THRESHOLD=0.6
AGENT_MAX_EXECUTION_TIME=30.0
AGENT_MAX_MEMORY_ENTRIES=1000

# Настройки API
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4

# Настройки логирования
LOG_LEVEL=INFO
LOG_JSON=true
LOG_FILE=logs/agent.log

# Настройки моделей
LOCAL_MODEL_PATH=models/llama-2-7b-chat.gguf
EMBEDDING_MODEL=all-MiniLM-L6-v2
```

## 🛠️ Установка внешних сервисов

### PostgreSQL (опционально)
```bash
# Установка через Docker
docker run -d \
  --name postgres-agent \
  -e POSTGRES_DB=agent_db \
  -e POSTGRES_USER=agent_user \
  -e POSTGRES_PASSWORD=secure_password \
  -p 5432:5432 \
  postgres:15-alpine
```

### Redis (обязательно для кэширования)
```bash
# Установка через Docker
docker run -d \
  --name redis-agent \
  -p 6379:6379 \
  --restart unless-stopped \
  redis:7-alpine
```

### Vector Database (ChromaDB)
```bash
# Установка через Docker
docker run -d \
  --name chroma-agent \
  -p 8000:8000 \
  ghcr.io/chroma-core/chroma:latest
```

## 🧪 Проверка установки

### Запуск тестов
```bash
# Запуск всех тестов
pytest tests/

# Запуск тестов с покрытием
pytest tests/ --cov=ai_agent_meta_cognitive

# Запуск конкретного теста
pytest tests/test_api_integration.py
```

### Запуск агента
```bash
# В режиме разработки
uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

# В продакшене
uvicorn api.main:app --host 0.0.0.0 --port 8000 --workers 4
```

### Проверка работоспособности
```bash
# Проверка API
curl http://localhost:8000/health

# Проверка статуса агента
curl http://localhost:8000/agent/status
```

## 📦 Установка моделей

### Локальные модели
```bash
# Скачивание и установка GGUF моделей
mkdir -p models
cd models

# Пример скачивания модели (требуется git-lfs)
git clone https://huggingface.co/TheBloke/Llama-2-7B-Chat-GGUF
```

### Установка эмбеддингов
```bash
# Установка предобученных эмбеддингов
pip install sentence-transformers
```

## 🔧 Дополнительная настройка

### Настройка кэширования
```python
# В config.py
CACHE_CONFIG = {
    "type": "redis",  # или "memory", "disk"
    "ttl": 3600,      # время жизни кэша в секундах
    "max_size": 1000  # максимальное количество записей
}
```

### Настройка базы данных
```python
# В config.py
DATABASE_CONFIG = {
    "url": os.getenv("DATABASE_URL"),
    "pool_size": 10,
    "max_overflow": 20,
    "echo": False  # для отладки установить True
}
```

## 🚨 Возможные проблемы и решения

### Проблемы с зависимостями
```bash
# Очистка кэша pip
pip cache purge

# Переустановка зависимостей
pip uninstall ai-agent-meta-cognitive
pip install -e .
```

### Проблемы с CUDA (для GPU)
```bash
# Установка версии PyTorch с поддержкой CUDA
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### Проблемы с доступом к API
- Проверьте правильность API ключей
- Убедитесь, что ключи не превысили лимиты
- Проверьте настройки брандмауэра

## 📝 Завершение установки

После успешной установки и настройки вы можете:

1. Проверить работоспособность агента
2. Настроить его под свои нужды
3. Интегрировать с вашими системами
4. Начать использовать мета-когнитивные возможности

Для начала работы ознакомьтесь с [руководством пользователя](USAGE.md).