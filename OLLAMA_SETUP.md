# Настройка Ollama для бесплатного AI агента

## Что такое Ollama?

Ollama - это инструмент для запуска больших языковых моделей локально на вашем компьютере. Это **полностью бесплатно** и не требует API ключей!

## Преимущества Ollama:

- ✅ **Бесплатно** - никаких платежей
- 🔒 **Приватно** - данные остаются на вашем компьютере
- 🚀 **Быстро** - работает локально без задержек интернета
- 💪 **Мощно** - поддерживает современные модели (Llama, Mistral, etc.)

## Установка Ollama

### Windows/macOS/Linux:

1. **Скачайте Ollama** с официального сайта:
   ```
   https://ollama.ai/download
   ```

2. **Установите** приложение для вашей ОС

3. **Запустите Ollama**:
   ```bash
   ollama serve
   ```

## Установка модели

После установки Ollama, скачайте модель:

```bash
# Рекомендуемая модель (хороший баланс качества и скорости)
ollama pull llama2

# Альтернативы:
ollama pull mistral        # Более качественная, но медленнее
ollama pull llama2:13b     # Лучшее качество, требует больше RAM
ollama pull codellama      # Специально для кода
```

## Проверка работы

```bash
# Проверьте установку
ollama list

# Вы должны увидеть:
# NAME        	ID          	SIZE  	MODIFIED
# llama2:latest	78e26419b446	3.8 GB	2 minutes ago
```

## Настройка AI агента

1. **Создайте .env файл**:
   ```bash
   cp .env.example .env
   ```

2. **Убедитесь, что OLLAMA_URL установлен** (по умолчанию):
   ```
   OLLAMA_URL=http://localhost:11434
   ```

3. **Запустите агента**:
   ```bash
   python -m uvicorn api.main:app --reload
   ```

## Тестирование

```bash
# Протестируйте интеграцию
python test_llm_integration.py

# Вы должны увидеть:
# ✅ Найдено 1 API ключей:
#    • Ollama
# 🔄 Тестирование Ollama...
# ✅ Ответ получен!
# 🤖 Модель: llama2 (Ollama)
# 🎯 Уверенность: 0.70
# ⏱️ Время: 2.34 сек
# 💬 Ответ: [умный ответ от Llama 2]
```

## Устранение проблем

### Ollama не запускается:

```bash
# Убейте процесс и перезапустите
taskkill /F /IM ollama.exe  # Windows
pkill ollama               # Linux/macOS

# Перезапустите
ollama serve
```

### Модель не скачивается:

```bash
# Проверьте интернет и повторите
ollama pull llama2

# Или попробуйте другую модель
ollama pull mistral
```

### Агент не подключается:

```bash
# Проверьте, что Ollama работает
curl http://localhost:11434/api/tags

# Должен вернуть JSON с моделями
```

## Производительность

### Системные требования:

- **RAM**: Минимум 4GB, рекомендуется 8GB+
- **Диск**: 4-8GB для моделей
- **CPU**: Современный процессор с AVX2

### Оптимизация:

```bash
# Используйте меньшую модель для старых ПК
ollama pull llama2:7b

# Или квантизированную версию
ollama pull llama2:7b-q4_0
```

## Альтернативы (если Ollama не подходит)

Если Ollama не работает на вашем ПК, используйте облачные бесплатные модели:

### Mistral AI (бесплатный tier):
```bash
# В .env файле:
MISTRAL_API_KEY=ваш_ключ_от_mistral
```

### Google Gemini (бесплатно):
```bash
# В .env файле:
GOOGLE_API_KEY=ваш_ключ_от_google
```

## Следующие шаги

1. **Запустите Ollama** и модель
2. **Настройте .env** файл
3. **Запустите AI агента**
4. **Наслаждайтесь бесплатным AI!** 🤖✨

## Поддержка

Если возникли проблемы:
- Проверьте логи Ollama: `ollama logs`
- Проверьте логи агента в терминале
- Убедитесь, что порт 11434 не занят
